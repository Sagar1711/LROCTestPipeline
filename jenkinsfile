pipeline {
    agent { dockerfile true }
    stages {
        stage('Build Env') {
            steps {
                sh 'cat /etc/os-release'
                sh 'gcc --version'
            }
        }
    }
}

