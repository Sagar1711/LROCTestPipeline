pipeline {
    agent { dockerfile true }
    stages {
        stage('Build Env') {
            steps {
                sh 'uname --all'
                sh 'gcc --version'
            }
        }
    }
}

