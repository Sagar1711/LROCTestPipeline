pipeline {
    agent { dockerfile true }
    stages {
        stage('Build Env') {
            steps {
                sh 'lsb_release -sirc'
                sh 'gcc --version'
            }
        }
    }
}

